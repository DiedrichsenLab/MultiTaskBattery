# Linux only: wxPython (needed by psychopy) has no Linux wheels on PyPI, so pip
# tries to compile it from source and fails. Prebuilt wheels exist per distro. Install.py will download the correct wheel for your distro and install it.
-f https://extras.wxpython.org/wxPython4/extras/linux/gtk3/${MTB_WX_DISTRO}/

psychopy==2026.1.3
pandas
numpy>=1.26.4
scipy
moviepy
imageio
ffmpeg-python
sounddevice
soundfile
pyyaml
questplus
pooch
numba
nibabel
psychopy-sounddevice
